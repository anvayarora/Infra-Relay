---
- name: AutoDriver precheck
  hosts: target
  gather_facts: true
  become: true
  tasks:
    - name: Run precheck stage
      ansible.builtin.include_role:
        name: autodriver_gpu
        tasks_from: precheck.yml
