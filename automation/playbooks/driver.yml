---
- name: AutoDriver driver
  hosts: target
  gather_facts: true
  become: true
  tasks:
    - name: Run driver stage
      ansible.builtin.include_role:
        name: autodriver_gpu
        tasks_from: driver.yml
