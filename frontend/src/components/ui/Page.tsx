import {motion} from 'framer-motion';import {spring} from '../../lib/utils';
export function Page({children}:React.PropsWithChildren){return <motion.main initial={{opacity:0,y:6}} animate={{opacity:1,y:0}} transition={spring} className="mx-auto w-full max-w-[1600px] px-6 py-6 lg:px-8">{children}</motion.main>}
export function PageHeader({eyebrow,title,description,actions}:{eyebrow?:string;title:string;description?:string;actions?:React.ReactNode}){return <div className="mb-7 flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div>{eyebrow&&<p className="eyebrow mb-2">{eyebrow}</p>}<h1 className="text-2xl font-semibold tracking-tight">{title}</h1>{description&&<p className="mt-1 max-w-3xl text-sm leading-6 text-zinc-500">{description}</p>}</div>{actions&&<div className="flex items-center gap-2">{actions}</div>}</div>}
