import {useEffect,useState} from 'react';import {motion} from 'framer-motion';import {History} from 'lucide-react';import {api} from '../lib/api';import {formatDate,spring} from '../lib/utils';import {Card} from '../components/ui/Card';import {Page,PageHeader} from '../components/ui/Page';
export function AuditPage(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{api.get('/audit').then(r=>setItems(r.data.items))},[]);return <Page><PageHeader eyebrow="Accountability" title="Activity" description="See who changed what, when it happened and which service was affected."/><Card className="overflow-hidden">{items.map((x,i)=><motion.div key={x.id} initial={{opacity:0,x:-5}} animate={{opacity:1,x:0}} transition={{...spring,delay:i*.02}} className="grid gap-4 border-b border-zinc-100 px-5 py-4 md:grid-cols-[1fr_1fr_1fr_auto] dark:border-zinc-900"><div className="flex gap-3"><History size={15}/><div><p className="text-xs font-medium">{x.action}</p><p className="text-[10px] text-zinc-500">{x.actor}</p></div></div><p className="font-mono text-[10px]">{x.target_type} / {x.target_id}</p><p className="truncate font-mono text-[10px] text-zinc-500">{JSON.stringify(x.payload)}</p><p className="text-[10px] text-zinc-500">{formatDate(x.created_at)}</p></motion.div>)}</Card></Page>}
