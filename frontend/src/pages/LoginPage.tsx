import { FormEvent, useState } from 'react'
import { Navigate } from 'react-router-dom'
import { motion } from 'framer-motion'
import { ArrowRight, Boxes, Command, Network, ShieldCheck } from 'lucide-react'
import { toast } from 'sonner'
import { useAuth } from '@/hooks/useAuth'
import { Button } from '@/components/ui/Button'
import { Input, Label } from '@/components/ui/Input'
import { getError, spring } from '@/lib/utils'

export function LoginPage(){const{user,login}=useAuth();const[email,setEmail]=useState('admin@infrarelay.local');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e:FormEvent){e.preventDefault();setLoading(true);try{await login(email,password)}catch(error){toast.error(getError(error))}finally{setLoading(false)}}return <div className="grid min-h-screen bg-zinc-50 lg:grid-cols-[1.05fr_.95fr] dark:bg-zinc-950"><div className="relative hidden overflow-hidden border-r border-zinc-200 p-12 lg:flex lg:flex-col dark:border-zinc-900"><div className="absolute inset-0 grid-bg opacity-70"/><div className="relative z-10 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-zinc-950 text-white dark:bg-white dark:text-zinc-950"><Network size={17}/></div><div><div className="text-sm font-semibold">InfraRelay</div><div className="text-[10px] uppercase tracking-[.18em] text-zinc-500">Infrastructure Workspace</div></div></div><div className="relative z-10 my-auto max-w-xl"><motion.div initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} transition={spring}><p className="eyebrow">Your infrastructure, your way</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.045em] text-zinc-950 dark:text-white">Turn infrastructure tasks into simple services.</h1><p className="mt-6 max-w-lg text-base leading-7 text-zinc-500">Create guided request flows for servers, accounts, bookings and GPU maintenance, then connect them to the interface your team already uses.</p></motion.div><div className="mt-10 grid grid-cols-3 gap-3">{[[Boxes,'Build visually'],[Command,'Use any frontend'],[ShieldCheck,'Approve safely']].map(([Icon,label])=>{const I=Icon as typeof Boxes;return <div key={String(label)} className="rounded-xl border border-zinc-200 bg-white/80 p-4 backdrop-blur dark:border-zinc-800 dark:bg-zinc-950/70"><I size={16}/><div className="mt-4 text-xs font-medium">{label as string}</div></div>})}</div></div><div className="relative z-10 text-xs text-zinc-500">Connection details stay protected and are never shown inside your automation canvas.</div></div><div className="flex items-center justify-center p-6"><motion.div initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} transition={spring} className="w-full max-w-sm"><div className="mb-8"><div className="mb-4 grid h-10 w-10 place-items-center rounded-xl bg-zinc-950 text-white lg:hidden dark:bg-white dark:text-zinc-950"><Network size={18}/></div><p className="eyebrow">Sign in</p><h2 className="mt-2 text-2xl font-semibold tracking-tight">Open your workspace</h2><p className="mt-2 text-sm leading-6 text-zinc-500">Use the demo sign-in for local testing. Connect your company sign-in before going live.</p></div><form onSubmit={submit} className="space-y-4"><div><Label>Email</Label><Input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div><Label>Password</Label><Input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Optional in demo mode"/></div><Button type="submit" className="w-full" loading={loading}>Continue <ArrowRight size={15}/></Button></form></motion.div></div></div>}
